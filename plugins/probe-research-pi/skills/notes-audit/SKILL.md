---
name: notes-audit
description: Renamed to `audit-team-note` - load that skill. This stub ships for one release so a CLI whose audit dispatch still says `notes-audit` resolves to something.
---
# notes-audit (renamed)

This skill is now `audit-team-note`; the note-editing method it used to carry is `edit-notes`. Load `audit-team-note` and follow it. Nothing else is here.
